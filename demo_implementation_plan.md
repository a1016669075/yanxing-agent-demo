# 星上大气及时反演智能体 Demo 最终实施方案

## 1. 目标定义

本 demo 的目标不是完整复现论文级系统，而是构建一个可运行、可解释、可演示的原型，证明下面这件事成立:

在星上资源约束下，遥感智能体可以根据任务、数据质量和预算自动选择工作流，对高风险大气异常区域执行及时反演，并在失败时做有限度的动态调整。

## 2. 设计收敛结果

我对方案做了多轮内部评审，最终保留的技术路线遵循 6 条原则:

1. `科学贴合`: 必须直接贴合“星上大气及时反演”而不是泛泛遥感。
2. `可见性强`: 导师能一眼看到预算约束、流程选择、结果输出和流程修复。
3. `最小闭环`: 第一版只做一个完整闭环，不做大而全平台。
4. `零依赖优先`: 尽量不用复杂安装，降低运行失败概率。
5. `可扩展`: 后续可以替换成真实模型和真实数据。
6. `可控演示`: 必须能稳定触发并展示一次失败修复。

在这套约束下，最终选择:

1. 用浏览器前端展示智能体决策过程。
2. 用零依赖 JavaScript 承载知识库、执行引擎和修复逻辑。
3. 用合成但物理语义合理的场景数据模拟星上约束。
4. 用规则化 agent 实现“知识驱动 + 动态修复 + 简化记忆”。

## 3. Demo 主题

`SatAtmo-Agent: 面向星上大气异常事件的资源约束自适应及时反演智能体`

## 4. Demo 要回答的问题

给定一景待处理卫星场景和任务约束，智能体需要回答:

1. 当前应该用哪条反演流程?
2. 是否需要只处理 Top-K 异常 ROI?
3. 当前流程会不会超预算?
4. 当前数据质量是否需要云掩膜或降级处理?
5. 当标准流程失败时，如何切换为替代流程?

## 5. 第一版功能边界

### 包含

1. 多个大气异常场景。
2. 延迟预算、功耗模式、下传策略等任务约束。
3. 小型工作流知识库。
4. 执行引擎与步骤级日志。
5. 两类以上动态修复。
6. 简化记忆模块。
7. Agent 与固定脚本基线对比。

### 不包含

1. 真正星上芯片部署。
2. 真正深度学习训练。
3. 大规模 benchmark。
4. 完整多智能体系统。

## 6. 技术路线

### 6.1 架构

前端界面负责:

1. 任务配置
2. 工作流可视化
3. 执行日志展示
4. 结果对比展示

核心引擎负责:

1. 场景建模
2. 工作流检索
3. 预算评估
4. 工具执行仿真
5. 动态修复
6. 记忆更新

### 6.2 数据表示

每个场景包含:

1. 事件类型
2. 云量
3. 辐射质量
4. ROI 列表
5. 优先级分数
6. 预算相关信息

### 6.3 工作流知识库

第一版提供 4 类模板:

1. `dust_priority`
2. `pollution_cloud_aware`
3. `budget_guarded_fast`
4. `degraded_heatmap_only`

### 6.4 动态修复策略

第一版固定做 3 类:

1. 云污染过高时插入 `cloud_mask`
2. 预算溢出风险时将标准反演替换为快速反演并限制 ROI 数量
3. 质量过低时将定量产品降级为异常热区产品

### 6.5 记忆策略

第一版使用浏览器 `localStorage` 保存简单经验规则:

1. 记录失败类型
2. 记录修复动作
3. 下次规划时优先复用历史修复建议

## 7. 为什么不用真实模型先起步

因为第一版 demo 的首要任务是证明“智能体工作流可行”，不是证明“单个反演模型最优”。只要工作流逻辑、预算约束、修复行为和结果解释成立，就已经足以支撑方向汇报。后续替换真实模型时，只需要把仿真工具替换为真实推理接口。

## 8. 交付物

最终交付包含:

1. 一份方案文档
2. 一个可直接运行的本地网页 demo
3. 一个零依赖静态服务脚本
4. 一份运行说明

## 9. 验收标准

如果 demo 能做到下面几点，就算成功:

1. 用户能选择场景和预算约束。
2. 系统能生成并展示工作流。
3. 系统能逐步执行流程并显示日志。
4. 系统能触发至少一次自动修复。
5. 系统能展示 agent 相对固定脚本的收益。
6. 系统能输出一段适合讲给导师听的解释。

## 10. 本轮实现策略

本轮直接实现可运行版本，不继续停留在方案讨论。
