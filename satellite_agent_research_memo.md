# 星上大气及时反演智能体 Demo 研究备忘录

## 1. 目标

本备忘录服务于一个很具体的目标:

证明“星上大气及时反演 + 遥感智能体”不是一个过于超前、无法落地的方向，而是可以先通过一个小型、闭环、可演示的 demo 做出原型验证。

这里的关键不是完整复现论文全部实验，而是先复现其核心思想:

1. 任务理解不是固定脚本，而是知识驱动的流程选择。
2. 反演执行不是单步推理，而是多步工具链协同。
3. 执行过程不是一次性成功，而是能在有限范围内自适应调整。

## 2. 两条文献线索

### 2.1 星上及时处理线

从晏星老师相关论文看，当前工作已经证明:

1. 星上低功耗芯片可以承载轻量深度学习模型。
2. 星上处理的价值在于“只处理关键区域”和“把时延压缩到预警可用范围”。
3. 当前系统仍然偏任务专用，通常围绕某一类污染物或某一类事件构建固定流程。

代表性结论:

1. `Revolutionizing Satellite Real-Time Air Pollution Alerts through New On-Orbit System-on-Chip Technology` 提出 SoC-POM，在 Himawari-8/9 上对 PM2.5、PM10、O3 做异常检测和异常区域定量反演，平均时延约 5.5 min，并强调只在异常区域执行反演以节省计算。
2. `Satellite On-Orbit Chip-Level Deep Learning Model for Real-Time Dust Storm Monitoring` 从标题、关键词和 DOI 元数据看，延续的是“星上、实时、低资源、沙尘监测”这条线，说明沙尘/气溶胶类事件是一个很自然的星上任务入口。
3. `Fast model inference and training on-board of Satellites` 说明“星上模型推理甚至少量训练”本身是可实现的，不再只是概念。
4. `A Lightweight and Adaptive Image Inference Strategy for Earth Observation on LEO Satellites` 说明在星上资源受限条件下，固定模型策略并不优，动态选择模型可以显著提升精度-能耗效率。

### 2.2 遥感智能体线

从 CangLing-KnowFlow、ThinkGeo、Earth-Agent 这一线可以看到:

1. 遥感智能体的核心不是聊天，而是“多步工作流规划 + 工具调用 + 动态纠错”。
2. 真正有效的遥感智能体依赖专家工作流知识库，而不是仅靠通用 LLM 自由发挥。
3. 最新工作已经开始从简单 RGB 感知走向光谱、产品、定量反演等更科学的任务。

代表性结论:

1. `CangLing-KnowFlow` 把智能体拆成 Orchestrator、Procedural Knowledge Base、Dynamic Workflow Adjustment、Evolutionary Memory 四部分，并用 1008 条流程模板、162 类任务、324 条 benchmark workflow 支撑评估。
2. `ThinkGeo` 说明遥感场景下，评估智能体不能只看最终答案，还要看多步工具使用过程和规划一致性。
3. `Earth-Agent` 进一步强调 EO 智能体需要跨模态、可调用专家工具，并且要支持 geophysical parameter retrieval 这类定量任务。

## 3. 当前最值得抓的研究缺口

把这两条线放在一起，最明显的缺口不是“能不能做智能体”，而是:

### 缺口 1: 现有星上系统大多是固定任务流程

当前星上方案往往是:

`输入影像 -> 固定检测模型 -> 固定反演模型 -> 固定输出`

这类系统高效，但不够灵活。面对云污染、低信噪比、异常区域形态变化、不同事件类型切换时，流程自适应能力有限。

### 缺口 2: 现有遥感智能体大多默认地面算力环境

现有遥感智能体论文主要关注:

1. 工具规划对不对
2. 工作流能不能跑通
3. 遇到错误能不能修

但它们通常没有把以下星上约束显式纳入决策:

1. 算力预算
2. 能耗预算
3. 时延上限
4. 下传带宽
5. 只对局部区域精细处理的必要性

### 缺口 3: 缺少“资源约束下的星上大气反演智能体”原型

也就是说，现在缺的不是单个模型，而是一个能回答下面问题的系统:

“在有限算力和时延约束下，应该对哪里做反演、做哪种反演、用哪条流程做反演、遇到数据质量问题后如何调整？”

这就是你最适合切入的地方。

## 4. 推荐的科学问题

### 4.1 大问题

在星上有限算力、能耗和带宽约束下，如何实现面向突发大气事件的自适应、及时、可靠的定量反演？

这个问题很大，直接做会过重。

### 4.2 推荐拆分后的小问题

更适合 demo 的小问题是:

在星上资源受限条件下，如何让智能体只对高风险大气异常区域触发定量反演，并根据数据质量和预算动态调整处理流程？

这句话很重要，因为它同时包含了:

1. 及时反演
2. 资源约束
3. 异常驱动
4. 工作流自适应

而且它和晏星老师两篇论文是自然衔接的，不会显得偏题。

## 5. 为什么这个问题适合先做 demo

因为它可以被拆成一个足够小、但又足够像“智能体”的闭环:

1. 先做异常检测，不全图盲目反演。
2. 再决定是否进入定量反演。
3. 再根据预算和数据质量选择轻量或标准流程。
4. 若流程失败，自动切换备选步骤。
5. 最后输出结果图、流程图和解释日志。

这已经不是普通分类模型了，而是一个最小化的“星上任务决策智能体”。

## 6. 推荐 demo 主题

建议把第一版 demo 定义成:

`面向沙尘/污染异常区域的星上自适应及时反演智能体原型`

更具体地说，可以做成下面这个版本:

### 方案名称

`OrbitAir-Agent` 或 `SatAtmo-Agent`

### demo 场景

输入一景卫星影像和一条任务指令，例如:

“请在 6 分钟预算内，对该景影像进行大气异常筛查，并优先完成高风险区域的沙尘/污染反演结果输出。”

### 智能体需要完成的动作

1. 识别任务类型: 沙尘事件优先，或污染异常优先。
2. 选择工作流模板: 标准流程、快速流程、低质量数据流程。
3. 调用工具: 预处理、云筛查、异常检测、ROI 裁剪、轻量反演、质量评估、结果生成。
4. 判断预算: 若预算紧张，则只对高置信 ROI 做反演。
5. 动态修复: 若云覆盖高、质量评分低或模型失败，则切换到备用流程。
6. 生成解释: 给出“为什么只反演这些区域、为什么选这条流程”。

## 7. Demo 中真正要解决的小技术问题

第一版不要追求“做出完整科学系统”，而要明确只解决下面这个小问题:

“在给定预算下，智能体如何从候选流程中选择一条可行流程，对异常区域完成优先级驱动的及时反演，并在简单失败条件下进行一次自动调整？”

这个问题够小，可以做出来。

## 8. 推荐的最小系统结构

### 8.1 Orchestrator

输入:

1. 用户任务描述
2. 当前预算约束
3. 数据基本信息

输出:

1. 工作流选择结果
2. 执行顺序
3. 调整决策

### 8.2 小型流程知识库

不需要 1008 条模板，第一版只要 6 到 10 条高质量模板即可。

建议模板类型:

1. 正常质量影像 -> 异常检测 -> ROI 反演
2. 云量较高 -> 云掩膜 -> ROI 反演
3. 预算紧张 -> 快速异常筛查 -> Top-K ROI 反演
4. 质量较差 -> 降级输出异常热区而非完整反演
5. 沙尘场景 -> 沙尘流程
6. 污染场景 -> 污染流程

### 8.3 工具层

第一版工具不要多，够用即可:

1. 影像读取与重采样
2. 简单云掩膜或质量筛查
3. 异常区域检测
4. ROI 裁剪
5. 轻量反演模型或代理反演模型
6. 质量评分
7. 可视化与报告生成

### 8.4 动态调整层

第一版只做两到三类修复就够:

1. 云量过高 -> 增加云掩膜步骤
2. 预算不足 -> 从全 ROI 反演切换为 Top-K ROI 反演
3. 结果质量低 -> 切换轻量模型或输出低阶产品

### 8.5 记忆层

第一版不需要真正做长期学习数据库。

只需做一个简化版:

1. 记录失败类型
2. 记录采用的修复动作
3. 下次遇到同类失败时优先复用该动作

这就已经足够接近论文思想。

## 9. 为什么这个 demo 对导师有说服力

因为它能够同时展示三件事:

1. 星上及时反演不是只能做固定脚本，可以做任务驱动决策。
2. 智能体不是空泛概念，而是能显式控制“异常检测 -> ROI 选择 -> 反演 -> 调整”的流程。
3. 未来可以很自然扩展到更多大气任务，比如 AOD、沙尘、臭氧、PM、多污染物联合监测。

换句话说，这个 demo 的价值不在于它已经“完成科学问题”，而在于它已经展示了解决这类科学问题的系统范式。

## 10. 一句话版研究命题

如果需要一个适合对导师汇报的题目，我建议用:

`面向星上大气异常事件的资源约束自适应及时反演智能体原型研究`

如果想更聚焦沙尘，可以改成:

`面向沙尘事件的星上资源约束自适应及时反演智能体原型研究`

## 11. 第一版里程碑

### 第 1 阶段: 文献与任务收敛

目标:

1. 定下一个小问题
2. 定下一个数据场景
3. 定下一个最小流程库

### 第 2 阶段: 跑通主流程

目标:

1. 输入任务
2. 选择流程
3. 完成异常检测和 ROI 反演
4. 生成结果

### 第 3 阶段: 加入动态调整

目标:

1. 构造一个失败场景
2. 自动切换流程
3. 输出修复前后对比

### 第 4 阶段: 包装展示

目标:

1. 可视化流程图
2. 结果图叠加展示
3. 输出解释文字

## 12. 当前建议

当前不建议直接追求:

1. 完整复现 KnowFlow-Bench
2. 完整复现 1000+ 工作流知识库
3. 真正上星硬件部署

当前最建议的是:

1. 先把科学问题收敛成“异常驱动 + ROI 反演 + 预算约束 + 流程调整”
2. 先做地面验证版智能体 demo
3. 用这个 demo 证明方向成立，再往真实星上部署约束逼近

## 13. 下一步可直接展开的工作

下一步最值得做的是两件事:

1. 把这个问题继续压缩成一个 2 到 4 周可完成的最小复现方案。
2. 选定一个具体任务场景，比如“沙尘异常优先反演”或“臭氧异常优先反演”，开始搭 demo 架构。
